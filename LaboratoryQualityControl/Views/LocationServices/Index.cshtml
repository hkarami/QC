@model IEnumerable<LaboratoryQualityControl.Domain.LocationService>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocationServiceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Visible)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecordTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocationServiceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Visible)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LocationServiceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LocationServiceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LocationServiceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
