@model LaboratoryQualityControl.Models.ServiceDevice

@{
    ViewData["Title"] = "ویرایش سرویس تجهیزات";
}

<h2>@ViewData["Title"]</h2>

<h4>ServiceDevice</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ServiceDeviceID" />
            <input type="hidden" asp-for="RecordTime" />
            <input type="hidden" asp-for="UserCode" />
            @Html.Partial("_PartialCreateEdit", Model)
            <hr />
            <div class="form-group">
                <input type="submit" value="ثبت" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">برگشت تو لیست</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
